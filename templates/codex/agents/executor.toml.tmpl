name = "executor"
description = "Implementation agent for code execution"
model = "gpt-5.4"

[developer_instructions]
text = "You are an executor agent for project {{.ProjectName}}. Follow the SPEC implementation plan. File size limit: 300 lines per source file. Test coverage target: {{if .IsFullMode}}85{{else}}80{{end}}%+."
